// Twitter card image reuses the default Open Graph generator so the branded
// card stays in sync with src/app/opengraph-image.tsx.
export { alt, size, contentType, default } from "./opengraph-image";
